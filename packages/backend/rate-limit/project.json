{
  "name": "backend-rate-limit",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/backend/rate-limit/src",
  "projectType": "library",
  "tags": ["scope:backend", "type:domain", "runtime:node"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/backend/rate-limit"],
      "options": {
        "command": "tsc -p packages/backend/rate-limit/tsconfig.lib.json"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p packages/backend/rate-limit/tsconfig.lib.json --noEmit"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/backend/rate-limit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run packages/backend/rate-limit/src --passWithNoTests"
      }
    }
  }
}
