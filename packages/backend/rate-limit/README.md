# Rate limiting

Framework-free rate-limit policies and storage ports. The API composes this boundary with an in-memory development store or the PostgreSQL adapter exported by `@agentic-webapp/database`.
