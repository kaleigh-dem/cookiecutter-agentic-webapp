{
  "cli": "nx",
  "$schema": "https://json-schema.org/schema",
  "title": "Shared contract generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Contract name",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What is the contract name?"
    },
    "skipFormat": {
      "type": "boolean",
      "default": false,
      "x-priority": "internal"
    }
  },
  "required": ["name"]
}
