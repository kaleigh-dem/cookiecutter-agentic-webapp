{
  "cli": "nx",
  "$schema": "https://json-schema.org/schema",
  "title": "Worker job generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Job name",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What is the job name?"
    },
    "queue": {
      "type": "string",
      "description": "Logical queue name",
      "default": "default"
    },
    "skipFormat": {
      "type": "boolean",
      "default": false,
      "x-priority": "internal"
    }
  },
  "required": ["name"]
}
