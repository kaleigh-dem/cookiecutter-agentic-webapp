{
  "cli": "nx",
  "$schema": "https://json-schema.org/schema",
  "title": "Frontend feature generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Feature name",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What is the feature name?"
    },
    "skipFormat": {
      "type": "boolean",
      "default": false,
      "x-priority": "internal"
    }
  },
  "required": ["name"]
}
