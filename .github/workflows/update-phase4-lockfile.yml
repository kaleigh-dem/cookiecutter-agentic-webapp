name: Update Phase 4 workspace

on:
  push:
    branches: [agent/add-data-foundation]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/add-data-foundation

      - uses: pnpm/action-setup@v4
        with:
          version: 10.13.1

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - run: pnpm install --lockfile-only --no-frozen-lockfile
      - run: pnpm nx sync
      - run: pnpm format

      - name: Commit normalized workspace
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Normalize Phase 4 workspace"
          git push origin HEAD:agent/add-data-foundation
