name: CI

on:
  pull_request:

permissions:
  contents: write

jobs:
  refresh-lockfile:
    if: >-
      github.event.pull_request.head.ref == 'agent/finish-phase-7-telemetry' &&
      github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/finish-phase-7-telemetry
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10.13.1

      - uses: actions/setup-node@v6
        with:
          node-version: 25
          cache: pnpm

      - run: pnpm install --no-frozen-lockfile
      - run: pnpm nx sync
      - run: pnpm format
      - run: pnpm telemetry:check

      - name: Commit generated workspace metadata
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add pnpm-lock.yaml tsconfig.json apps/web/tsconfig.json
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "chore(telemetry): refresh generated metadata"
          git push origin HEAD:agent/finish-phase-7-telemetry
