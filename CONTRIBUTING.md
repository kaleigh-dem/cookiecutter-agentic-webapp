# Contributing

1. Create a focused branch from `main`.
2. Read the root and nearest nested `AGENTS.md`.
3. Use Nx generators for new projects and repeated structures.
4. Run `pnpm nx affected -t lint typecheck test build`.
5. Explain boundary, runtime dependency, migration, and rollout changes in the pull request.
