{
  "name": "{{ cookiecutter.package_scope }}/database",
  "private": true,
  "scripts": { "build": "tsc", "lint": "eslint src", "typecheck": "tsc --noEmit", "test": "vitest run" },
  "devDependencies": { "typescript": "^5.8.3", "vitest": "^3.2.4" }
}
