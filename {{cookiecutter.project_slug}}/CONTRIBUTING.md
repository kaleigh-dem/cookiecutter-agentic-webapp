# Contributing

Create focused branches and pull requests. Keep commits intentional and avoid mixing refactors with behavior changes.

Before opening a pull request, run:

```bash
pnpm check
```

Pull requests should explain what changed, why, the affected module boundaries, validation performed, and any rollout or migration considerations.
